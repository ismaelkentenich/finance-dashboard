services:
  finance-dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: finance-dashboard-app
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
